import { db } from '../firebase/config';

const handleFollowing = (payload) => {
    return new Promise(async (resolve, reject) => {
        try {
        } catch (error) {
            reject(error);
        }
    });
};

export { handleFollowing };
